Nothing Spira ships may name an operator, a machine, or one box's paths — those are config keys with defaults, and comments address the operator, never a person. A statute ships if it is about the machinery, and stays if it is about our history. The published repo must clone and run; test-freshclone.sh is the positive control that makes that enforceable rather than assumed.