A passing pull request must merge without a human. Enable repository auto-merge and arm it when you open the request; a green request that waits is not merely idle, it rots — the base moves underneath it and manufactures conflicts that did not exist. Six sat overnight with auto-merge disabled at the repository level, two of them green, and the wait alone turned them dirty. Never file a rebase you could do; the waiting caused it.
