A gate, check or test invoked by automation must run in an explicit minimal environment, never the caller's. Ambient configuration silently decides verdicts: a scheduler exported a host setting, systemd passed it into a test suite asserting defaults, so correct work was rejected and would have been rejected on every retry until the bead poisoned — presenting as a failing bead with nothing pointing at the environment. Pass what the check needs and nothing else.
