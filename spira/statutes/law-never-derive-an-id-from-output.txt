Never derive an id by grepping human-readable command output; ask for --json. A tool may print an advisory that echoes the title before the id, so a grep for a bare id pattern can return a DIFFERENT record: a pattern stopping at the first hyphen returned the parent epic, and the delete that followed removed it with all seven of its dependency edges, every command exiting 0. Close a record to finish it; deleting is not reversible.
