A test that asserts a bug REPRODUCES measures the machine as much as the code, so it is not done until CI has run it green at least once. Reproduction usually needs state a fresh CI workspace lacks — a primed build cache, an idle box, warm artifacts — and a developer's machine has all three. Two changes shipped such assertions verified only locally, never passed CI once, and blocked the queue for days.
